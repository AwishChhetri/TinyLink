<!DOCTYPE html>
<html>
  <head>
    <title>Stats - TinyLink</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
      @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap');
      * { font-family: 'Inter', sans-serif; }
    </style>
  </head>

  <body class="bg-gradient-to-br from-slate-900 via-slate-800 to-slate-900 min-h-screen text-slate-100">
    <header class="border-b border-slate-700/50 bg-slate-900/50 backdrop-blur-sm sticky top-0 z-50">
      <div class="max-w-5xl mx-auto px-4 sm:px-6 py-4">
        <h1 class="text-2xl font-bold bg-gradient-to-r from-blue-400 to-cyan-400 bg-clip-text text-transparent">✨ TinyLink</h1>
      </div>
    </header>
    <div class="max-w-xl mx-auto mt-16 bg-slate-800/50 border border-slate-700/50 backdrop-blur-sm p-8 rounded-2xl shadow-xl">

      <h1 class="text-2xl font-bold mb-6 bg-gradient-to-r from-blue-400 to-cyan-400 bg-clip-text text-transparent">
        📊 Stats for <%= link.code %>
      </h1>

      <div class="space-y-4 text-sm">

        <div class="p-4 bg-slate-900/40 rounded-lg border border-slate-700/60">
          <p class="text-slate-400 text-xs uppercase mb-1">Target URL</p>
          <p class="truncate text-blue-300"><%= link.url %></p>
        </div>

        <div class="p-4 bg-slate-900/40 rounded-lg border border-slate-700/60">
          <p class="text-slate-400 text-xs uppercase mb-1">Total Clicks</p>
          <p class="text-lg font-semibold"><%= link.clicks %></p>
        </div>

        <div class="p-4 bg-slate-900/40 rounded-lg border border-slate-700/60">
          <p class="text-slate-400 text-xs uppercase mb-1">Last Clicked</p>
          <p><%= link.last_clicked || "—" %></p>
        </div>

        <div class="p-4 bg-slate-900/40 rounded-lg border border-slate-700/60">
          <p class="text-slate-400 text-xs uppercase mb-1">Short URL</p>
          <p class="text-cyan-300"><%= `${link.base_url || ''}/${link.code}` %></p>
        </div>

      </div>

      <a href="/" 
        class="mt-8 inline-block px-4 py-2 bg-slate-700 hover:bg-slate-600 rounded-lg text-sm font-medium transition">
        ← Back to Dashboard
      </a>
    </div>

  </body>
</html>
